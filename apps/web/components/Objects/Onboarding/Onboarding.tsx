import React from 'react'

/**
 * Onboarding component is currently disabled.
 * To re-enable, restore the original implementation from version control.
 */
const Onboarding: React.FC = () => {
  return null
}

export default Onboarding
